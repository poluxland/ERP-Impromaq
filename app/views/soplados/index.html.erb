<p id="notice"><%= notice %></p>

<h1>Registro soplado</h1>


<table id="myTable" class="table table-striped table-bordered table-sm shadow">
  <thead>
    <tr>
      <th>Supervisor</th>
      <th>Fecha</th>
      <th>Presión ducto ascendente</th>
      <th>Presión ducto descarga CC54</th>
      <th>Presión Quench</th>
      <th>Alimentación Horno</th>
      <th>Observaciones</th>
      <th></th>
      <th></th>


    </tr>
  </thead>

  <tbody>
    <% @soplados.each do |soplado| %>
      <tr>
        <td><%= soplado.supervisor %></td>
        <td><%= soplado.fecha.strftime("%Y/%m/%d") %></td>
        <td><%= soplado.presion1 %></td>
        <td><%= soplado.presion2 %></td>
        <td><%= soplado.presion3 %></td>
        <td><%= soplado.horno %></td>
        <td><%= soplado.observaciones %></td>
        <td><%= link_to 'Ver', soplado %></td>
        <td><%= link_to 'Eliminar', edit_soplado_path(soplado) %></td>

      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nuevo registro', new_soplado_path, class: "btn btn-primary shadow" %>
